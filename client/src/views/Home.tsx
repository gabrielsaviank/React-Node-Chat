import React, { useEffect } from "react";

import { useAuth } from "../context/AuthProvider";
import Api from "../api/Api";

const Home = () => {

    return(
        <div>
            <h1>HomeScreen</h1>
        </div>
    );
};

export default Home;